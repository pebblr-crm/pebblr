Pebblr CRM has been deployed!

  Release:   {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}
  Version:   {{ .Chart.AppVersion }}

To access the API:
{{- if .Values.ingress.enabled }}
  {{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
  {{- end }}
{{- else if eq .Values.service.type "ClusterIP" }}
  kubectl port-forward svc/{{ include "pebblr.fullname" . }} 8080:{{ .Values.service.port }} -n {{ .Release.Namespace }}
  Then visit: http://localhost:8080
{{- end }}

Health checks:
  Liveness:  GET /healthz
  Readiness: GET /readyz

Secrets are delivered via ExternalSecret → Azure KeyVault → file mounts.
Ensure the SecretStore ({{ .Values.externalSecrets.secretStoreName }}) is configured
and the managed identity has GET access to the KeyVault.
