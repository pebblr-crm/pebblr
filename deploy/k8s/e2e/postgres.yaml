# PostgreSQL 16 for E2E testing.
# Not for production — single-replica, ephemeral storage, no backups.
apiVersion: v1
kind: Namespace
metadata:
  name: pebblr-e2e
---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: pebblr-e2e
type: Opaque
stringData:
  POSTGRES_USER: pebblr
  POSTGRES_PASSWORD: pebblr-e2e-password
  POSTGRES_DB: pebblr
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: pebblr-e2e
  labels:
    app: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: postgres-credentials
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "pebblr"]
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: pebblr-e2e
  labels:
    app: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres
