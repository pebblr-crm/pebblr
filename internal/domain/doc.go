// Package domain defines the core business entities for the pebblr CRM.
// These types are shared across the application and have no dependencies
// on infrastructure (HTTP, database, etc.).
package domain
