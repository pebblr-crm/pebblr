// Package auth provides Azure AD (Entra ID) OIDC authentication.
// Tokens are validated against the configured tenant and audience.
// Claims are extracted and attached to the request context for downstream use.
package auth
